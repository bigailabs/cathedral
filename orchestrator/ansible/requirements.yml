---
collections:
  - name: ansible.builtin
    version: ">=2.11.0"

  - name: ansible.posix
    version: ">=1.5.0"

  - name: community.general
    version: ">=6.0.0"

  - name: ansible.utils
    version: ">=2.0.0"

  - name: kubernetes.core
    version: ">=2.4.0"
