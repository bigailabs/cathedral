---
dependencies:
  - role: prereq
  - role: download
  - role: k3s_custom_registries
    when: custom_registries | default(false) | bool

galaxy_info:
  author: Basilica Team
  description: K3s agent node installation
  license: MIT
  min_ansible_version: "2.11"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
