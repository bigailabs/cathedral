# Railway reads service config from the repository root. Keep this in sync with
# deploy/railway.toml so CLI uploads and GitHub deploys use the hardened image.

[build]
builder = "dockerfile"
dockerfilePath = "deploy/Dockerfile"

[deploy]
healthcheckPath = "/health/live"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Do not set numReplicas here while this Railway service has an attached volume:
# Railway rejects multi-replica deployments for volume-backed services. Scale
# with separate read/submit services instead of replicas on this service.
