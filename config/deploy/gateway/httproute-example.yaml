apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rental-example
  namespace: u-test
spec:
  parentRefs:
    - name: basilica-gw
  hostnames:
    - demo.u-test.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: rental-svc-demo
          port: 8888

