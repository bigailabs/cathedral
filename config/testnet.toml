[network]
name = "test"
netuid = 292
validator_hotkey = "REPLACE_ME"
wallet_name = "cathedral-validator"

[polaris]
base_url = "https://api.polaris.computer/"
public_key_hex = "REPLACE_WITH_POLARIS_ED25519_PUBLIC_KEY_HEX"
fetch_timeout_secs = 20

[http]
listen_host = "0.0.0.0"
listen_port = 9333
bearer_token_env = "CATHEDRAL_BEARER"

[weights]
interval_secs = 600
disabled = false
# SN292 testnet: uid 0 is the subnet owner (hotkey 5C5GQGiMqBEB...).
# Mainnet SN39 uses uid 204 (also the owner). Same burn-to-owner pattern,
# different uid because testnet has 40 neurons not 256.
burn_uid = 0
forced_burn_percentage = 98.0
v3_bug_isolation_weight = 0.0
task_family_weights = { synthetic_boolean_v1 = 0.0 }

[weight_source]
mode = "local"
publisher_weights_url = "https://api.cathedral.computer/v1/validator/weights/next"
poll_interval_secs = 60.0
request_timeout_secs = 10.0
fallback_after_stale_minutes = 10.0
refuse_after_stale_minutes = 30.0
cathedral_policy_public_key_env = "CATHEDRAL_POLICY_PUBLIC_KEY_HEX"
cathedral_policy_key_id = "cathedral-weight-policy"

[publisher]
url = "https://api.cathedral.computer"
public_key_env = "CATHEDRAL_PUBLIC_KEY_HEX"
pull_interval_secs = 30.0
api_token_env = "CATHEDRAL_PUBLISHER_TOKEN"

[storage]
database_path = "data/validator-testnet.db"

[worker]
poll_interval_secs = 5.0
max_concurrent_verifications = 4

[stall]
after_secs = 600
